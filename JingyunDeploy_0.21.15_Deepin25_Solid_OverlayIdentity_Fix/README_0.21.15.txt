菁云镜像部署系统 0.21.15 Deepin 25 Solid修改层个性化修复版（无HTTP、无数据库）

0.21.15关键修复：
1. 修正0.21.14对Deepin 25磁盘结构的判断：Deepin 25并不是直接从/ostree/deploy中的系统层启动，实际的/etc、/usr、/opt由OverlayFS的系统层、数据层和上下修改层组合。
2. 个性化不再直接修改OSTree系统部署目录，避免设置被上层覆盖，也避免改动硬链接对象导致OSTree仓库校验异常。
3. 自动识别/persistent/overlay/data在独立持久化分区或系统分区内的布局，同时写入：
   - 上修改层 layer-*/etc；
   - 下修改层 数据部署ID/etc-upper。
4. 计算机名直接写入Deepin实际可见的/etc/hostname修改层；固定IP/DHCP直接生成绑定目标MAC的NetworkManager keyfile，自动连接优先级999。
5. 首次启动服务同样写入Deepin的上下修改层，并改为After=NetworkManager、Before=network-online，避免旧IP或无网络时等待network-online导致服务不执行。
6. /var使用任务ID完成标记，即使Deepin无忧还原重新生成上修改层，也不会重复执行；下修改层中的名称和网络配置仍然保留。
7. Deepin扫描范围扩展到普通分区、LVM、加密映射、软RAID及Btrfs顶层子卷，并在挂载点内限深搜索overlay/data，兼容持久化层位于独立分区或嵌套目录的情况。
8. 首次启动新增持久化诊断日志：/var/lib/zos/deploy-identity-任务ID.log；失败时不再只依赖临时日志，便于现场直接定位NetworkManager或服务启动问题。
9. x86_64、ARM64、LoongArch64三套维护环境都已更新；镜像任务仍保持新任务在最上方。

原0.21.14说明（问题版本，仅保留变更记录）：

0.21.14曾做的调整：
1. 修复Deepin 25/25.2“Solid不可变系统”镜像下发成功，但部署后计算机名和IP不生效的问题。
2. Linux个性化会识别OSTree真实部署根目录：ostree/deploy/*/deploy/*.0，并同时检查sysroot、rootfs等挂载层级。
3. 对检测到的全部OSTree部署写入计算机名与首次启动网络配置，兼顾当前部署及回滚部署，避免下次切换部署后丢失设置。
4. 不可变系统的一次性个性化脚本改放在可写的/etc/zos目录，systemd服务不再依赖只读的/usr/local/sbin。
5. NetworkManager没有现成有线连接配置时，会按注册MAC新建专用连接，再应用固定IP、网关、DNS或恢复DHCP。
6. x86_64、ARM64、LoongArch64三套维护启动环境均已重新嵌入0.21.14代理；本包仍不包含RISC-V维护启动环境。
7. “镜像任务”列表改为倒序显示，新创建和最近更新的任务显示在最上方，底层任务数据和执行逻辑不变。

0.21.13完善：
1. “已注册客户端”列表支持点击任意列标题排序；再次点击同一标题可在升序和降序之间切换。
2. 注册IP按IPv4数值排序，硬盘数按数字排序，客户端名称采用自然排序（例如PC-2排在PC-10前面）。
3. 每秒刷新客户端状态时保留当前排序列、排序方向和已选客户端，不会自动跳回注册时间顺序。
4. 排序后批量修改客户端身份，严格按照界面当前可见顺序识别所选客户端，不会再按后台注册文件原顺序错配。
5. 批量修改IP和计算机名新增“正序/倒序”：正序从当前列表最上方开始分配起始编号和起始IP，倒序从最下方开始分配。
6. 本版只修改管理端列表和批量身份编辑逻辑；x86、ARM64、龙芯启动镜像及写盘代理保持0.21.11验证版本不变。

0.21.12完善：
1. “已注册客户端”中的IP改为唯一的管理端注册IP；PXE临时通信IP单独记录，不再覆盖管理员设置的IP。
2. “镜像任务”列表按任务MAC实时关联已注册客户端，客户端名称和IP与注册列表保持一致。
3. 新建MAC定向上传/下发任务时，立即写入对应注册名称和IP，客户端尚未开机领取任务也能正确显示。
4. 新增“修改IP/计算机名”按钮：选择1台可直接修改；Ctrl/Shift选择多台可按列表顺序批量生成名称和连续IP。
5. 批量名称支持前缀、起始编号和编号位数；批量IP支持起始IP并按当前子网连续分配，自动阻止网络地址、广播地址和越界地址。
6. 修改注册身份后，尚未领取的定向任务会同步更新名称、任务IP及已勾选的部署后计算机名/固定IP参数。
7. 正在执行或已经领取的任务禁止中途修改身份，避免写盘期间配置发生变化；重复IP会在修改时被阻止。
8. 手工修改或导入的名称/IP会被锁定保存，客户端以后再次PXE注册时只更新在线通信IP、硬盘信息和最后在线时间，不再覆盖管理员设置。
9. 本版只修改管理端身份数据和界面逻辑，x86、ARM64、龙芯写盘代理继续使用0.21.11已验证版本，保留0.21.10长时间写盘SIGPIPE修复。


现场故障为：141733920768字节源盘下发时，写到22150660096字节、约72秒后，
socat报Broken pipe，状态为transport=1、zstd=141、dd=141、written=0。
这次不是“完整写完后的141误报”，而是ARM64启动环境中的进度解析命令提前退出，
进度管道缓冲区随后被写满，反向把正在写盘的dd杀成SIGPIPE，继而让zstd和socat连锁退出。

根因：
1. ARM64 FOS上游initramfs中的gawk和gunzip原本使用硬链接；
2. 旧版dependency-free cpio重建工具没有保留硬链接，把/usr/bin/gawk重建成0字节文件；
3. 0.21.9把dd的stderr直接接入tee | tr | awk进度解析管道；空gawk立即退出后，
   管道不是立刻报错，而是在几十秒后缓冲区写满，最终dd收到SIGPIPE 141并中途停止；
4. 小容量/高速任务可能在缓冲区写满前完成，所以0.21.9曾表现为“偶尔成功”，大镜像更容易稳定复现。

0.21.11完善：
1. 下发写盘进度改为 MiB（兆）显示，不再显示超长字节数；
2. 写盘进度使用同一行刷新，完成或失败后自动换行，不再连续刷出多行；
3. 设置选中客户端自动下发时，保留覆盖硬盘确认，确认后直接创建任务，不再弹出第二个“设置完成”窗口；
4. 镜像任务区恢复“删除任务”和“清空任务”按钮；清空不会删除 images 目录中的镜像文件；
5. x86、ARM64 与龙芯启动镜像均已嵌入新版代理，继续保留 0.21.10 的长时间写盘 SIGPIPE 修复。

0.21.10修复：
1. dd进度不再通过进程替换管道解析，改为写入普通状态文件；任何进度解析异常都不会影响镜像数据流；
2. 后台进度线程使用/bin/busybox awk读取状态文件，继续每3秒向管理端上报实际写盘字节和耗时；
3. ARM64 initramfs内的/usr/bin/gawk已恢复为gawk-5.3.2链接，/bin/gunzip已恢复为gzip链接；
4. S99启动脚本增加运行时自检修复，即使旧构建文件再次出现0字节别名也会在代理启动前纠正；
5. cpio_newc工具新增标准newc硬链接保留逻辑，后续从上游initramfs重建不会再制造空命令；
6. 单播和x86/ARM UDPcast组播都改用安全的普通文件进度采集；
7. 保留0.21.9的完整性判定：只有zstd=0、dd=0且实际写入不少于源盘容量才成功，短写绝不放行；
8. 已重新生成包内tftp/arm64/zos/init.cpio.gz，客户端无需手工替换脚本。

菁云镜像部署系统 0.21.9 ARM64完整写盘误报修复版（无HTTP、无数据库）

现场验证显示ARM64已成功领取任务，并将源镜像全部22040360448字节写入目标磁盘；但zstd完成解压并关闭管道后，socat收到SIGPIPE 141，旧代理因set -o pipefail将整个任务误判为失败，导致没有继续执行sync、个性化和任务完成上报。

0.21.9修复：
1. 单播和组播下发分别记录请求端、传输端、zstd解码器和dd写盘器的独立退出状态；
2. 只有zstd=0、dd=0且写入字节不少于镜像源盘容量时，才允许认定镜像完整；
3. 若完整镜像已写入，而socat/传输端仅在帧结束后收到SIGPIPE 141，则作为正常流结束继续处理；
4. zstd错误、dd错误、短写或请求发送失败仍严格判定为失败，不会掩盖损坏镜像；
5. 控制台新增完整传输状态，便于区分网络、解压和磁盘写入问题；
6. 写盘成功后继续执行sync、分区表重读、计算机名/IP个性化和完成上报。

菁云镜像部署系统 0.21.8 ARM64 iPXE静态网络交接修复版（无HTTP、无数据库）

本版纠正0.21.6/0.21.7的网络修复方向：当iPXE已经使用192.168.50.x地址成功下载内核和initramfs时，Linux接管网卡后必须继续使用同一地址，不能再次运行DHCP。现场中二次DHCP被另一台服务器172.16.0.1响应，将客户端改成172.16.201.x，导致无法连接192.168.50.150:8090。

0.21.8修复：
1. iPXE提供有效IP和掩码时，按静态地址恢复并立即写入/run/zos-network.env；
2. 地址添加成功后直接信任iPXE地址，不再因BusyBox ip输出格式不同而误判失败；
3. 有iPXE地址的启动流程完全禁止DHCP回退，避免被其他DHCP服务器改到错误网段；
4. 为管理端IP增加固定主机路由，保证注册通信始终走PXE网卡；
5. S99fog和注册代理优先读取已确认的网络状态，不再依赖ip -o，也不在TCP失败时重启网络；
6. DHCP仅在iPXE没有传入地址时使用；x86、龙芯、组播、镜像和个性化逻辑未修改。

菁云镜像部署系统 0.21.6 通用ARM64网络交接修复版（无HTTP、无数据库）
=================================================

运行：
  pip install PyQt5 psutil
  以管理员/root权限运行：python jingyun_simple_manager.py

多架构引导（本版重点）：
  - 一个管理端同时提供x86/x86_64、ARM64和龙芯LoongArch64三套独立维护环境。
  - DHCP按UEFI架构号发送对应iPXE；进入iPXE后再按buildarch选择内核和initrd，禁止跨架构误启动。
  - x86/x86_64：成熟功能，支持注册、RAW整盘上传/下发、单播、组播及部署后个性化。
  - ARM64：使用Linux 6.18.38通用维护内核和FOG FOS根文件系统，已内置ACPI、PL011、
    通用PCI Host、VirtIO网卡/磁盘/SCSI/GPU、EFI framebuffer及gzip/XZ/Zstd initramfs支持；
    支持注册、RAW整盘上传/下发、单播和组播，兼容QEMU/Proxmox virt及常见UEFI/ACPI ARM64服务器。
  - 龙芯LoongArch64：采用openEuler 24.03 LTS LoongArch64 PXE内核/initrd，支持注册、
    自动磁盘枚举、RAW整盘单播/可靠组播上传下发、逐台进度回报及Linux部署后个性化。
  - 龙芯可靠组播使用ZOSMC1窗口确认协议，不依赖LoongArch64版udp-receiver：每个窗口必须由
    全部客户端ACK；丢失的数据包按NACK重发，最终核对压缩文件总字节和SHA-256。
  - 龙芯Linux个性化支持ext2/3/4、XFS、Btrfs及LVM根分区，支持计算机名、固定IP、
    DHCP、网关和DNS；网络设置由按MAC定位网卡的一次性systemd服务在首次启动应用。
  - 龙芯不执行x86 Windows个性化；x86 Windows镜像不能在龙芯CPU启动。
  - 新上传镜像记录源CPU架构，已知架构不一致时管理端和客户端都会禁止写盘。
  - 管理程序本身可在Windows或Linux运行；ARM64/龙芯Linux需安装对应架构的Python 3、PyQt5和psutil。
    若要由非x86管理端发起组播，还需要系统PATH中存在该架构原生udp-sender。
  - 本版不包含ARM32、MIPS或32位龙芯维护环境。

多架构TFTP文件：
  - x86_64：tftp/x86_64/zos/bzImage + init.xz
  - ARM64：tftp/arm64/zos/Image + init.cpio.gz
  - LoongArch64：tftp/loongarch64/zos/vmlinuz + initrd.xz
  - ARM64和龙芯都需要UEFI网络启动；旧式BIOS的undionly.kpxe仅用于x86。

上传镜像：
  1. 选择连接客户端的网卡，点击“开启 PXE + DHCP”。
  2. 客户端先PXE启动并选择“Register this computer”完成注册。
  3. 管理端选中该客户端，点击“设置选中客户端任务（上传/下发）”。
  4. 选择“下次开机自动上传模板”，填写镜像名称、源盘和完成后动作。
  5. 客户端再次PXE启动后自动领取定向上传任务。
  6. 完成后镜像保存为 images/*.img.zst，旁边同时保存安全校验元数据。

下发镜像：
  1. 客户端先完成注册，管理端选中该客户端。
  2. 点击“设置选中客户端任务（上传/下发）”，选择“下次开机自动下发镜像”。
  3. 选择已上传的 RAW 整盘镜像；目标设备通常保持 auto。
  4. 客户端再次PXE启动后自动领取定向下发任务。
  5. 客户端自动识别可写物理盘并校验容量；目标盘不小于源盘才会开始写入。

客户端注册与自动任务：
  1. 管理端先填写客户端分组（逗号分隔），第一项为默认组，并点击“保存客户端分组”。
  2. 未注册客户端PXE启动后选择“Register this computer”。
  3. 客户端名称直接回车使用系统主机名；分组直接回车使用管理端默认组。
  4. 分组输入 ? 可查询管理端分组，再输入序号选择。
  5. 注册完成后，客户端可选择 1 重启或 2 关机。
  6. MAC、名称、组、主机名、IP和磁盘信息会写入管理端。
  7. 管理端选择客户端，再点击“设置选中客户端任务”。
  8. 可设置“下次开机自动上传模板”或“下次开机自动下发镜像”。
  9. 自动任务绑定客户端MAC并且只执行一次，其他客户端不会误领。
  10. 没有自动任务时，PXE菜单默认倒计时10秒从本地硬盘启动；秒数可在管理端修改。
  11. RAW上传和下发时，客户端屏幕显示已处理字节、传输速度和实时进度。

分组任务、批量唤醒和导出：
  1. 在“列表筛选”中选择具体分组，点击“设置当前组任务”。
  2. 组上传会为每台客户端建立独立镜像任务，名称可使用 {group}、{hostname}、{mac}、{date}。
  3. 组部署可选择“单独下发”或“组播同步下发”，都使用同一个RAW整盘镜像。
  4. 单独下发仍通过TCP 8090逐台独立传输；各客户端互不等待。
  5. 组播同步下发会等本次组任务中的所有客户端PXE上线，并完成目标盘和容量检查。
  6. 管理端显示已就绪数量；少一台都不会启动udp-sender，也不会开始覆盖客户端硬盘。
  7. 全部就绪后只发送一条可靠组播流；x86/ARM使用UDPcast，龙芯使用ZOSMC1，
     客户端通过丢包重传协议接收并显示写盘进度。
  8. 可选择“千兆高速（推荐）”“兼容稳定”或“高速网络/SSD”三种组播速度。
  9. 部署任务表按每台MAC分别显示实际写盘进度、已写入容量、开始时间和耗时。
  10. 组播使用会话独立的两个UDP端口（任务信息中会显示）；交换机和VLAN需允许组播通信。
  11. 客户端列表支持Ctrl/Shift多选，再点击“唤醒选中客户端”；也可“唤醒当前组”。
  12. WOL通过所选部署网卡向当前子网广播地址的UDP 7和9端口重复发送。
  13. 客户端须在BIOS和网卡中启用网络唤醒，通常要求有线网卡和同一二层网络。
  14. “导出客户端列表”支持Excel .xlsx和UTF-8 TXT；选择分组时只导出当前组。
  15. “导入客户端”支持.xlsx、UTF-8/GB18030的.txt和.csv；至少需要MAC列，
      可同时包含客户端名称、IP和组。再次导入同一MAC会更新原记录，不会产生重复客户端。
  16. 客户端和任务列表均支持Ctrl/Shift多选及“全选”；可批量删除。
      删除客户端时取消其待执行任务，但保留历史任务和镜像；正在执行的客户端/任务禁止误删。
      删除组播任务时会按完整组播会话一起删除，镜像文件不会随任务记录删除。

现代化界面：
  - 使用深蓝侧栏、白色信息卡片、粉蓝强调色和统一表格样式，风格接近现代管理控制台。
  - PXE启停集中在左侧，网络、客户端、任务和日志仍在同一窗口中，不改变原有操作流程。

按注册信息分别设置名称和静态IP（可选）：
  1. 新建单机或分组“下发镜像”任务时，“修改计算机名”和“设置固定IP”可分别勾选。
  2. 未勾选修改计算机名时保留镜像原名称；未勾选固定IP时明确恢复IPv4和DNS自动获取，
     即使模板原来使用固定IP也不会沿用旧地址。
  3. 每台任务仍以MAC地址定向；名称和IP分别取该MAC在“已注册客户端”里的名称和IP。
  4. Windows名称会转换为ASCII安全名称并限制为15个字符；不合规名称自动使用ZOS加MAC后6位。
  5. Windows 7及以上版本采用ZOS早期离线个性化逻辑：镜像写盘后、Windows启动前，
     直接修改离线SYSTEM注册表中的实际ControlSet、ControlSet001/002/003，无需安装客户端。
  6. Linux镜像写完后按所选项目修改；固定IP或恢复DHCP均使用一次性systemd服务按MAC找到网卡，
     支持NetworkManager、network-scripts、systemd-networkd和ifupdown。
  7. 如果Windows分区因快速启动/休眠而不能读写，或系统分区格式不受支持，镜像下发仍会完成，
     任务状态显示“完成但个性化失败”，不会误报为全部成功。
  8. 计算机名直接写入SYSTEM注册表并在写回后重新读取验证；这部分完全不依赖Windows登录、
     PowerShell、计划任务或常驻服务，兼容Windows 7、8/8.1、10、11。
  9. 静态IP优先直接写入离线SYSTEM注册表：从活动ControlSet中查找持有DHCP租约的接口GUID，
     写入EnableDHCP=0、IPAddress、SubnetMask、DefaultGateway和NameServer。
  10. 固定IP任务始终写入一次性PowerShell 2.0兼容校验动作，按注册MAC匹配实际网卡，
      设置并复核IP、网关和DNS，成功后自动删除，不安装常驻代理。
  11. 如果NTFS处于休眠/快速启动状态，ZOS先尝试ntfs-3g和NTFS3挂载；挂载不可用时使用
      ntfscat/ntfscp直接读取、修改、写回并复核SYSTEM注册表文件。
  12. 银河麒麟V10部署个性化支持LVM自动激活及XFS nouuid安全挂载；所有挂载操作有超时保护。
      首次启动服务使用/bin/bash执行并兼容未激活的NetworkManager连接，个性化异常只标记警告，
      已完成的镜像写盘仍会正常报告并执行重启/关机动作。

Linux兼容范围：
  - RAW整盘上传和下发不依赖发行版，GPT/MBR、EFI和所有分区会原样复制。
  - 部署后“修改计算机名/固定IP”已覆盖银河麒麟V10、Ubuntu/Debian、UOS/Deepin、
    CentOS/RHEL/Rocky/Alma和openSUSE/SLES常用布局；支持ext2/3/4、XFS、Btrfs，
    以及NetworkManager、network-scripts、systemd-networkd、ifupdown。
  - 0.21.15改为识别Deepin 25/25.2 Solid OverlayFS上下修改层；此前仅定位OSTree部署根目录的方案会被上层覆盖，
    将一次性服务和脚本写入可写的/etc，并按注册MAC设置计算机名、固定IP或DHCP。
  - LUKS加密根分区、ZFS根分区、特殊软RAID及其他未识别的不可变布局仍不能保证离线个性化；
    遇到这类系统可不勾选名称/IP，镜像本身仍可正常上传和下发。

网络默认值：
  - 网关：服务器IP前三段不变，最后一段默认为254；可删除留空。
  - DNS1：223.6.6.6；可删除留空。
  - DNS2：114.114.114.114；可删除留空。
  - DHCP会同时下发填写的网关和两个DNS；固定IP个性化也使用相同设置。

组成：
  - DHCP：程序内置，UDP 67。
  - TFTP：程序内置，UDP 69；iPXE菜单、ZOS内核和init.xz全部通过TFTP读取。
  - 镜像上传/单独下发：独立TCP 8090，不是HTTP。
  - 组播下发：TCP 8090只负责任务与就绪协调；x86/ARM镜像由UDPcast传输，
    龙芯镜像由内置ZOSMC1可靠组播传输。
  - 任务：data/tasks.json。
  - 终端：data/nodes.json。
  - 注册终端：data/registrations.json。
  - 镜像：images/*.img.zst。
  - 没有SQLite、SQL、MariaDB、Werkzeug、Flask或HTTP服务。

安全规则：
  - 只有管理端已有排队的“下发”任务，并且客户端主动选择 Deploy，才会写盘。
  - 当前只下发 RAW 整盘镜像，因此GPT/MBR、EFI分区、系统分区和数据分区一并恢复。
  - 目标盘全部现有分区和数据会被覆盖，不能撤销。
  - 目标盘必须与源盘同容量或更大；更大的多余空间暂时保持未分配。
  - 多硬盘机器建议在任务中明确目标设备，避免自动选择不符合预期。

注意：
  完整DHCP只能用于独立部署网络或隔离VLAN，不能与现有DHCP服务器同时使用。
  Windows运行时需要管理员权限；Linux运行时需要root权限。
  客户端和服务器的TCP 8090必须互通。
  0.10.1修复下发请求发送完后socat过早结束连接、导致zstd报告premature end的问题。
  0.11.0新增客户端自助注册、MAC定向一次性开机任务、无任务本地启动倒计时、客户端传输进度，
  并将管理端PXE日志改为最新记录显示在最上方。
  0.11.1在管理端主窗口增加“已注册客户端”列表。选择客户端后点击
  “设置选中客户端任务”，再指定上传模板或部署镜像。
  0.12.0为每个上传/部署任务增加“成功后自动重启、自动关机、停留维护界面”选项。
  只有任务成功并向管理端确认后才执行自动动作，失败时保留错误画面。
  管理端日志保持最新记录在最上方；界面、PXE路径和客户端提示统一使用ZOS名称。
  0.13.0新增管理端客户端分组配置。客户端注册时可输入名称，直接回车使用默认名称；
  分组直接回车使用默认组，输入 ? 可读取管理端分组并按序号选择。
  注册成功后可在客户端选择重启或关机，管理端客户端列表显示所属组。
  0.14.0将上传和下发任务统一合并到“设置选中客户端任务（上传/下发）”。
  删除两个独立的新建任务按钮和PXE菜单中的非定向上传/下发入口，任务必须绑定已注册客户端。
  ZOS启动环境的默认主机名改为zosclient.localdomain，登录提示和客户端名称不再使用旧名称。
  0.15.0新增按组筛选、设置当前组任务、唤醒选中客户端、唤醒当前组和客户端列表导出。
  组任务会为每台客户端建立独立MAC定向任务；任务表增加分组列。
  导出支持无需额外依赖的标准XLSX和TXT格式。
  0.16.0新增组部署的“单独下发/组播同步下发”选择。组播模式建立一个共享会话，
  只有本次任务中的全部客户端都完成上线和目标盘检查后才启动可靠UDPcast传输。
  Windows x86_64和Linux x86_64管理端已随包携带udp-sender；其他Linux架构会查找
  系统PATH中的udp-sender。ZOS x86_64启动环境已携带udp-receiver。
  随包third_party目录保留UDPcast对应源代码，许可证见tools/udpcast/COPYING。
  0.17.0优化UDPcast组播窗口、分片、缓冲区和8MiB写盘块，并新增三种速度档位。
  管理端按每台MAC显示实际落盘进度、容量、开始时间和耗时，而不是只显示共享网络流状态。
  下发任务可选按已注册客户端的名称和IP为Windows/Linux部署结果生成首次启动配置。
  0.17.1修复部分Windows部署后名称和静态IP未生效：按Select配置写入实际活动ControlSet，
  新增延迟LocalSystem服务、Run及启动目录三重重试，处理Fast Startup休眠NTFS状态；
  Windows和Linux首次启动完成后向管理端回报真实个性化结果。
  0.17.2修复ZOS的BusyBox环境错误使用“mount -t ntfs-3g”导致Windows分区返回
  “No such device”的问题。改为直接调用ntfs-3g用户态程序，自动准备FUSE设备，
  处理休眠状态，并在FUSE不可用时回退到内核NTFS3驱动。
  0.17.3新增完全不依赖挂载和FUSE的Windows直接写入方案。常规NTFS挂载失败时，
  使用ntfscat直接取出SYSTEM/SOFTWARE注册表配置，修改后通过ntfscp写回，
  并把首次启动程序写入现有的C:\Windows\Temp目录后进行回读验证。
  0.18.1纠正0.18.0的Windows模板客户端方向，移除Windows客户端安装包和领取接口。
  Windows 7及以上计算机名改为ZOS早期离线SYSTEM注册表修改；
  静态IP使用执行后自删的一次性脚本，不安装常驻代理。通信仍是独立TCP+JSON文件，
  不引入HTTP、SQL、MariaDB、Flask或Werkzeug。
  0.18.2修复计算机名已修改但静态IP仍为DHCP的问题。ZOS现在从离线SYSTEM注册表中
  自动识别持有DHCP租约的活动网卡GUID，并直接写入注册客户端的静态IPv4、掩码、
  网关和DNS；管理端任务状态显示“已离线写入”。一次性脚本仅作为更换网卡后的兜底。
  0.18.3修复0.18.2的init.xz误用多线程分块和CRC64，导致旧版启动内核提示
  “XZ decoder不支持编码设置”并无法挂载/dev/ram0的问题。启动环境恢复为与稳定版一致的
  单数据块、CRC32、LZMA2 64MiB字典格式；离线计算机名和静态IP功能保持不变。
  0.18.4修复RAW镜像换机后Windows重新生成网卡接口GUID、导致离线静态IP落在旧接口的问题。
  除离线注册表写入外，始终写入一次性Windows 7+兼容校验动作；首次登录时按注册MAC找到
  实际网卡、设置并复核静态IPv4，成功后自动删除启动入口和脚本，不安装常驻代理。
  0.18.5修复部分chntpw/reged版本已打印“OK”并成功修改SYSTEM、但进程仍返回非零状态，
  导致ZOS误报个性化失败并跳过静态IP脚本的问题。所有SYSTEM、静态IP和SOFTWARE导入
  现在都以重新导出注册表并核对目标值为准，不再以reged退出码判断成功或失败。
  0.19.0将“修改计算机名”和“设置固定IP”拆分为两个独立选项，单机与分组任务均支持。
  未勾选名称时保留镜像名称，未勾选固定IP时强制恢复DHCP和自动DNS。网络设置新增可留空的网关、DNS1、
  DNS2；默认分别为服务器IP末段254、223.6.6.6和114.114.114.114，并同时用于DHCP与部署个性化。
  0.20.0新增客户端列表导入、客户端批量删除、任务批量删除和现代化卡片式界面。
  0.20.1优化Windows管理端启动PXE+DHCP：网卡接口编号只解析一次并由DHCP与
  ProxyDHCP复用，删除重复网络校验，所有PowerShell探测均隐藏窗口。点击启动时不再
  弹出CMD窗口，且避免重复等待；麒麟、其他Linux个性化与组播流程保持不变。
  修复银河麒麟V10在勾选计算机名或固定IP时，因LVM/XFS根分区未激活或挂载阻塞而无法完成部署的问题。
  0.21.0新增真实ARM64与LoongArch64内核/initrd，并按DHCP架构号和iPXE buildarch双重路由。
  ARM64维护环境复用FOG FOS ARM64工具链并运行ZOS代理；龙芯维护环境基于openEuler官方
  LoongArch64 PXE initrd，首版提供无需jq/socat的原生Python ZOS代理，实现注册、RAW整盘TCP上传/下发。
  未知架构不再错误回落到龙芯启动文件。具体支持边界和上游校验值见third_party/UPSTREAM_MULTIARCH.txt。
  0.21.1新增不依赖外部二进制的ZOSMC1龙芯可靠组播：全组上线握手、窗口ACK/NACK补包、
  逐台实时写盘进度、压缩镜像字节/SHA-256终检。新增龙芯Linux部署后名称与网络个性化，
  支持LVM、ext系列、XFS和Btrfs，并按MAC在首次启动设置固定IP/DHCP、网关和DNS。
  新上传镜像写入source_arch元数据，任务领取和创建时双重阻止跨CPU架构错误下发。

  0.21.6重编译并真正替换ARM64维护内核为Linux 6.18.38通用版。关键驱动全部静态内置，
  不依赖客户机系统模块：ACPI、EFI Stub、PL011、通用PCI Host、VirtIO PCI/MMIO、
  VirtIO网卡/块设备/SCSI/GPU、simpledrm、EFI framebuffer以及gzip/XZ/Zstd initramfs。
  ARM UEFI引导使用有名称的initrd并在内核命令行传递initrd=，同时输出到tty0、ttyAMA0和ttyS0。
  修复Buildroot各启动脚本进程间环境变量不继承导致注册代理未启动的问题；S99fog会重新读取
  /proc/cmdline。网卡枚举改用/sys/class/net，避免VirtIO网卡尚未配置IP时被遗漏。


0.21.6 ARM64网络交接修复：
  1. iPXE将已验证可用的客户端IPv4、掩码、网关和DNS传给Linux维护环境。
  2. Linux优先恢复同一MAC的iPXE租约，避免内核接管网卡后第二次DHCP失败。
  3. iPXE未提供地址时，自动回退到udhcpc DHCP，并明确指定Buildroot租约脚本。
  4. 注册程序启动前强制检查IPv4；TCP 8090连接失败时自动重配网络并重试6次。
  5. 控制台显示接口、IPv4、配置来源和路由，便于区分客户端网络与管理端端口问题。

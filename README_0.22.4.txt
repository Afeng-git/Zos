菁云镜像部署系统 0.22.5

本版重点修复：
1. 离线部署网络不再尝试 apt/yum 在线安装 udpcast。
2. 龙芯组播使用内置 ZOSMC 可靠组播，不再错误要求 udp-sender。
3. ARM64/x86 的 UDPcast 组播只从随包目录或系统 PATH 查找 udp-sender；缺失时明确提示离线组件位置。
4. 执行中/已领取任务支持管理员确认后强制删除；异常关机、断网后不再卡死任务记录。
5. 清空任务遇到执行中任务时可二次确认强制清空；镜像文件不会删除。

注意：强制删除仅清理任务记录并停止对应组播发送会话，不会回滚已经写入客户端磁盘的数据。
